<Window x:Class="Frontend.Windows.Presentation.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="ServiceProcesses - Tickets" Height="750" Width="1200"
        WindowStartupLocation="CenterScreen"
        Background="#D9D9D9">

    <Grid>
        <Grid x:Name="AuthScreen" Visibility="{Binding IsAuthenticated, Converter={StaticResource InverseBoolToVisConverter}}">
            <Border Background="White" CornerRadius="15" Padding="40" 
                    VerticalAlignment="Center" HorizontalAlignment="Center" 
                    Width="450" BorderBrush="#CCC" BorderThickness="1">
                <Grid>
                    <StackPanel Visibility="{Binding IsLoginView, Converter={StaticResource BoolToVisConverter}}">
                        <TextBlock Text="ВХОД СОТРУДНИКА" FontSize="22" FontWeight="Bold" Margin="0,0,0,25" HorizontalAlignment="Center" Foreground="#333"/>
                        
                        <TextBlock Text="Логин" Margin="0,0,0,5" Foreground="#666"/>
                        <TextBox Text="{Binding LoginEmail, UpdateSourceTrigger=PropertyChanged}" Height="35" Margin="0,0,0,15" VerticalContentAlignment="Center" Padding="5,0"/>
                        
                        <TextBlock Text="Пароль" Margin="0,0,0,5" Foreground="#666"/>
                        <PasswordBox x:Name="LoginPasswordBox" PasswordChanged="LoginPasswordBox_OnPasswordChanged" Height="35" Margin="0,0,0,20" VerticalContentAlignment="Center" Padding="5,0"/>
                        
                        <Button Content="ВОЙТИ" Command="{Binding LoginCommand}" Height="45" Style="{StaticResource MenuButtonStyle}" Background="#4A4A4A" Foreground="White" HorizontalContentAlignment="Center"/>
                        <TextBlock Text="Desktop предназначен только для сотрудников. Клиентские сервисы вынесены в web/mobile." Margin="0,16,0,0" TextAlignment="Center" Foreground="#666" TextWrapping="Wrap"/>
                        <TextBlock Text="Текущий стенд: admin / admin" Margin="0,8,0,0" TextAlignment="Center" Foreground="#666" TextWrapping="Wrap"/>
                        <TextBlock Text="{Binding StatusText}" Margin="0,12,0,0" TextAlignment="Center" Foreground="#8A2D2D" TextWrapping="Wrap"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>

        <Grid x:Name="MainDashboard" Visibility="{Binding IsAuthenticated, Converter={StaticResource BoolToVisConverter}}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="220"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="320"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="#E0E0E0" BorderBrush="#CCCCCC" BorderThickness="0,0,1,0">
                <DockPanel Margin="15">
                    <StackPanel DockPanel.Dock="Top">
                        <TextBlock Text="РАБОЧЕЕ МЕСТО СОТРУДНИКА" Foreground="#888" FontWeight="Bold" Margin="0,0,0,20" FontSize="12"/>
                        <Button Content="Очередь заявок" Style="{StaticResource MenuButtonStyle}"/>
                        <Button Content="Операционный контроль" Style="{StaticResource MenuButtonStyle}"/>
                        <Button Content="Наряды и SLA" Style="{StaticResource MenuButtonStyle}"/>
                    </StackPanel>
                    
                    <Button DockPanel.Dock="Bottom" Content="Выйти" Command="{Binding LogoutCommand}" Background="Transparent" BorderThickness="0" Foreground="#C0392B" FontWeight="SemiBold"/>
                </DockPanel>
            </Border>

            <Grid Grid.Column="1" Margin="25">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="280"/>
                </Grid.RowDefinitions>

                <StackPanel Margin="0,0,0,20">
                    <TextBlock Text="{Binding WorkspaceTitle}" FontSize="26" FontWeight="SemiBold" Foreground="#1A1A1A"/>
                    <TextBlock Text="{Binding WorkspaceSubtitle}" Foreground="#666"/>
                    <TextBlock Text="{Binding CurrentUserName}" Foreground="#333" FontWeight="SemiBold"/>
                    <TextBlock Text="{Binding CurrentUserRole}" Foreground="#666"/>
                </StackPanel>

                <DataGrid Grid.Row="1" 
                          ItemsSource="{Binding Tickets}" 
                          SelectedItem="{Binding SelectedTicket}"
                          AutoGenerateColumns="False"
                          Background="White">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="60"/>
                        <DataGridTextColumn Header="Объект" Binding="{Binding ObjectName}" Width="*"/>
                        <DataGridTextColumn Header="Статус" Binding="{Binding Status}" Width="100"/>
                        <DataGridTextColumn Header="Приоритет" Binding="{Binding Priority}" Width="100"/>
                    </DataGrid.Columns>
                </DataGrid>

                <StackPanel Grid.Row="2" Margin="0,20,0,0">
                    <TextBlock Text="СТАТУСНАЯ ДОСКА СОТРУДНИКА" FontWeight="Bold" Margin="0,0,0,10" Foreground="#666"/>
                    <UniformGrid Columns="3" Height="220">
                        <Border Background="#CCC" Margin="5" CornerRadius="8">
                            <TextBlock Text="Новые" HorizontalAlignment="Center" Margin="10" VerticalAlignment="Center"/>
                        </Border>
                        <Border Background="#CCC" Margin="5" CornerRadius="8">
                            <TextBlock Text="В работе" HorizontalAlignment="Center" Margin="10" VerticalAlignment="Center"/>
                        </Border>
                        <Border Background="#CCC" Margin="5" CornerRadius="8">
                            <TextBlock Text="Завершено" HorizontalAlignment="Center" Margin="10" VerticalAlignment="Center"/>
                        </Border>
                    </UniformGrid>
                </StackPanel>
            </Grid>

            <Border Grid.Column="2" Background="#F5F5F5" Margin="10" CornerRadius="15">
                <StackPanel Margin="20">
                    <TextBlock Text="{Binding SelectedTicket.Id, FallbackValue='#0000'}" FontSize="24" FontWeight="Bold" Foreground="#333"/>
                    <TextBlock Text="{Binding SelectedTicket.ObjectName, FallbackValue='Выберите тикет для просмотра'}" TextWrapping="Wrap" Margin="0,10" Foreground="#666"/>
                    <Separator Margin="0,10"/>
                    <TextBlock Text="Операционные заметки" FontWeight="SemiBold" Margin="0,5"/>
                    <TextBox Height="150" Margin="0,10,0,0" VerticalContentAlignment="Top" 
                             AcceptsReturn="True" TextWrapping="Wrap" 
                             Background="White" Padding="5"/>
                    <TextBlock Text="{Binding StatusText}" Margin="0,12,0,0" Foreground="#666" TextWrapping="Wrap"/>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</Window>